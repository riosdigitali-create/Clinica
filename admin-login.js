(()=>{'use strict';const form=document.getElementById('access-form'),input=document.getElementById('access-code'),error=document.getElementById('access-error'),button=form.querySelector('button');input.addEventListener('input',()=>{const digits=input.value.replace(/\D/g,'').slice(0,6);input.value=digits.replace(/(\d{2})(?=\d)/g,'$1-');error.textContent=''});form.addEventListener('submit',async event=>{event.preventDefault();button.disabled=true;error.textContent='Verificando…';try{const response=await fetch('/api/admin-login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:input.value})});if(!response.ok){error.textContent='El código no es correcto. Inténtalo nuevamente.';input.select();form.classList.remove('denied');requestAnimationFrame(()=>form.classList.add('denied'));return}location.replace('/admin')}catch{error.textContent='No pudimos verificar el acceso. Inténtalo nuevamente.'}finally{button.disabled=false}})})();